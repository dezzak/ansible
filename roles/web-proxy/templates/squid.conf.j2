acl localnet src {{ localnet_ipv4_cidr }}
acl localnet src {{ localnet_ipv6_cidr }}

http_access allow localnet
http_access allow localhost

url_rewrite_program /usr/bin/squidGuard –c /etc/squid/squidGuard.conf
